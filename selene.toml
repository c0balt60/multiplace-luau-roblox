std = "roblox"

exclude = ["*_packages/**", "out/**", ".pesde/**", ".typedefs/**", "build/**"]

[lints]
mixed_table = "allow"
incorrect_standard_library_use = "allow"
multiple_statements = "allow"

[config]
global_usage = { ignore_pattern = "__[a-zA-Z0-9_]+__" }
